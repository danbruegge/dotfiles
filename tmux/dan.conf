new         -s dan -n env
send        -t env 'cd ~/workspace/danbrueggecom/' Enter
neww        -n dev
neww        -n bash

send        -t env 'yarn dev' Enter

send        -t dev 'cd ~/workspace/danbrueggecom/' Enter
send        -t dev 'vi' Enter

send        -t bash 'cd ~/workspace/danbrueggecom/' Enter
send        -t bash 'git i' Enter

selectw     -t env
selectp     -t 0
