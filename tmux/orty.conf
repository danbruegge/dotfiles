new         -s orty -n env
send        -t env 'cd ~/workspace/orty/app/' Enter
neww        -n dev
neww        -n bash

send        -t env 'yarn dev' Enter

send        -t dev 'cd ~/workspace/orty/app/' Enter
send        -t dev 'vi' Enter

send        -t bash 'cd ~/workspace/orty/app/' Enter
send        -t bash 'git i' Enter

selectw     -t env
selectp     -t 0
