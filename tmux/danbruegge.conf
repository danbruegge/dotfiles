new         -s danbruegge -n env
send        -t env 'cd ~/workspace/projects/danbruegge.com/' Enter
neww        -n dev

send        -t env 'make run' Enter

send        -t dev 'cd ~/workspace/projects/danbruegge.com/' Enter
send        -t dev 'nvim' Enter

selectw     -t env
selectp     -t 0
