new         -s mlws -n env
send        -t env 'cd ~/workspace/aesd/mondialab-website/' Enter
neww        -n dev

send        -t env 'make run' Enter

send        -t dev 'cd ~/workspace/aesd/mondialab-website/' Enter
send        -t dev 'nvim' Enter

selectw     -t env
selectp     -t 0
